@model LibraryCenter.Models.Shelf

@{
    ViewData["Title"] = "CreataShelf";
}

<h1>הוסף מדף חדש</h1>

<h4>Shelf</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddNewShelf">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div>
                <label for="LibraryId">Library ID</label>
                <input asp-for="Library" name="libraryId" id="LibraryId" />
            </div>
                  <input hidden="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Height" class="control-label"></label>
                <input asp-for="Height" class="form-control" />
                <span asp-validation-for="Height" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="הוסף מדף" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
